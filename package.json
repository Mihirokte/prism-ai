{
  "name": "prism-monorepo",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@10.28.2",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "typescript": "~5.5.0",
    "vitest": "^2.1.0",
    "turbo": "^2.3.0",
    "eslint": "^9.0.0",
    "prettier": "^3.4.0"
  }
}
